name: Daily US Stock Report

on:
  schedule:
    - cron: '0 23 * * *' # 台灣時間早上 7 點執行
  workflow_dispatch:      # 允許手動按按鈕執行

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      # 👇 這是你要新增的 FFmpeg 安裝步驟 (Linux 指令)
      - name: Install FFmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Install libraries
        run: |
          pip install -r requirements.txt

      - name: Run Bot
        env:
          # 左邊是程式碼用的變數 : 右邊是 GitHub Secrets 的名字
          GEMINI_API_KEY: ${{ secrets.GOOGLEGEMINIAPI_KEY }}
          LINE_TOKEN: ${{ secrets.LINE_TOKEN }}
          GROUP_ID: ${{ secrets.GROUP_ID }}
        run: python main.py
