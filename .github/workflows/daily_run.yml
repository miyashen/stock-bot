name: Daily US Stock Report

on:
  schedule:
    - cron: '0 23 * * *'
  workflow_dispatch:      # 👈 這行就是讓按鈕出現的關鍵

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      # 👇【新增這一段】安裝 FFmpeg 影音工具
      - name: Install FFmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Install libraries
        run: |
          pip install -r requirements.txt

      - name: Install libraries
        run: |
          pip install -r requirements.txt

      - name: Run Bot
        env:
          # 左邊不動 : 右邊是你剛剛確認過的正確鑰匙名
          GEMINI_API_KEY: ${{ secrets.GOOGLEGEMINIAPI_KEY }}
          LINE_TOKEN: ${{ secrets.LINE_TOKEN }}
          GROUP_ID: ${{ secrets.GROUP_ID }}
        run: python main.py
